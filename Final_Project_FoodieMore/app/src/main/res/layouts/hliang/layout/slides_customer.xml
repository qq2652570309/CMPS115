<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:background="@drawable/sea_1"
    android:layout_height="match_parent">
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/be_customer_200"
        android:layout_above="@+id/textView4"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="10dp" />
    <TextView
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:text="I want to order"
        android:textColor="#000000"
        android:textSize="25sp"
        android:id="@+id/textView4"
        android:layout_above="@+id/merchant_start_button"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="49dp" />

    <Button
        android:id="@+id/merchant_start_button"
        android:layout_width="130dp"
        android:layout_height="50dp"
        android:text="Start"
        android:onClick="onCustomerOnclick"
        android:background="@drawable/customer_start_button"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="94dp" />
</RelativeLayout>